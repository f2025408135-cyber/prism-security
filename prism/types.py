"""Type aliases used across the PRISM framework."""

# Type aliases representing unique identifiers
PrincipalId = str
EndpointUrl = str
StateName = str
PrimitiveId = str
FindingId = str

# Type aliases for nested data structures
HeadersDict = dict[str, str]
QueryDict = dict[str, str | list[str]]
